{
  "name": "evalcase",
  "version": "0.1.0",
  "description": "Deterministic, assertion-based evaluation of recorded AI-agent outcomes.",
  "type": "module",
  "bin": {
    "evalcase": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test test",
    "build": "rm -rf dist && bun build src/cli.ts --outfile dist/cli.js --target bun --format esm",
    "demo": "bun src/cli.ts demo",
    "verify": "bun run typecheck && bun run test && bun run build"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/node": "24.5.2",
    "typescript": "5.9.2"
  }
}
