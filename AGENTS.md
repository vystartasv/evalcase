# evalcase

## Commands

- `bun install --frozen-lockfile`
- `bun run verify` — typecheck, authoritative `bun test test`, and build
- `bun run demo`

Keep the runtime dependency-free and preserve deterministic diagnostics, report JSON, and exit codes. Do not add model calls, network access, sandboxing, or framework adapters.
